document.addEventListener("DOMContentLoaded",()=>{["campaignSearch","categoryFilter","sortFilter"].forEach(id=>document.getElementById(id)?.addEventListener("input",renderCampaignResults));const params=new URLSearchParams(location.search);const cat=params.get("category");if(cat&&document.getElementById("categoryFilter")){document.getElementById("categoryFilter").value=cat;renderCampaignResults();}});